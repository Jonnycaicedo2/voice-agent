"""LLM response processor."""


class LLMProcessor:
    """Generates text responses using an LLM."""

    async def generate(self, prompt: str) -> str:
        """Generate a response for the given prompt."""
        return ""
